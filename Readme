# Stillr Visualizer

**Versie:** 7.10 3D-Export

## Snelle GitHub & deploy stappen

1. **Zet je JPG/PNG-bestanden in de hoofdmap**
   - Plaats al je stofafbeeldingen (`.jpg` of `.png`) rechtstreeks in de projectmap naast `app.py`.

2. **Voeg de bestanden toe en push naar GitHub**
   ```bash
   cd stillr_app
   # Voeg alle JPG/PNG-bestanden toe
   git add *.jpg *.png
   # Voeg de overige wijzigingen toe (app.py, README.md, requirements.txt)
   git add app.py README.md requirements.txt

   git commit -m "Voeg stofafbeeldingen toe en update readme"
   git push origin main
